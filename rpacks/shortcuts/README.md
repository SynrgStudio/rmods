# shortcuts

Exact search aliases for launching favorite targets.

## Add a shortcut

1. Search for a normal launcher item.
2. Select it.
3. Press `Ctrl+B`.
4. Type the alias after `/shortcuts::bind `.
5. Press Enter.

User shortcuts are saved to `modules/shortcuts/shortcuts.user.json`.
